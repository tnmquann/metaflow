version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"
  jobs:
    post_install:
      - pip install shibuya

python:
  install:
    - method: pip
    - requirements: docs/requirements.txt

sphinx:
  builder: "dirhtml"
  configuration: docs/src/conf.py